#include<iostream>
#include<string>
using std::cout;
using std::cin;
using std::string;

int main() {
	int n;
	cin >> n;
	int i = 2;
	while (n != 1) {
		while (n % i == 0) {
			n /= i;
			cout << i << "\n";
		}
		i++;
	}
}
