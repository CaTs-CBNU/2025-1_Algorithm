#include<iostream>
#include<string>
using std::cout;
using std::cin;
using std::string;
int main() {
	int n;
	cin >> n;
	int sum = 0;
	for (int i = 1; i <= n; i++) {
		sum += i;
	}
	cout << sum;

	return 0;
}
