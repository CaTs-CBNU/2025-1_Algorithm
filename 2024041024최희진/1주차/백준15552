#include<iostream>
#include<string>
#include<sstream>
using std::cout;
using std::cin;
using std::string;
int main() {
    //ios::sync_with_stdio(false);
    cin.tie(NULL);

	int t;
	cin >> t;
	for (int i = 0; i < t; i++) {
		int A, B;
		cin >> A >> B;
		cout << A + B << "\n";
	}

	return 0;
}
