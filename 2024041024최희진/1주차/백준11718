#include<iostream>
#include<string>
#include<sstream>
using std::cout;
using std::cin;
using std::string;
int main() {
	string str;
    while (getline(cin, str)) {				
	    cout << str << "\n";
    }

	return 0;
}
